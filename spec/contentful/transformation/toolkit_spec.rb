# frozen_string_literal: true

RSpec.describe Contentful::Transformation::Toolkit do
  it "has a version number" do
    expect(Contentful::Transformation::Toolkit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
